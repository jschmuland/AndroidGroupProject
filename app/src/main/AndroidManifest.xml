<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comjschmulandandroidgroupproject.httpsgithub.androidgroupproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize|stateHidden">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MealPlanner"
            android:windowSoftInputMode="adjustResize"
            android:isScrollContainer="true"/>
        <activity
            android:name=".MealActivity"
            android:isScrollContainer="true"/>
        <activity
            android:name=".SleepTracker"
            android:label="@string/title_activity_sleep_tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Exercise"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".support.Exercise_info_class" />
        <activity android:name=".support.ExerciseMessageDetails" />
        <activity
            android:name=".FoodTracker"
            android:label="@string/title_activity_food_tracker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FoodPicker"
            android:label="@string/title_activity_food_picker"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>